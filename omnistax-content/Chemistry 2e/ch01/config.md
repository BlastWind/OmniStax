# Config: Chemistry 2e, Chapter 1

Proposed by the agent after the chapter exploration (2026-09-12). Status:
applied as proposed on 2026-09-12, on Chen's instruction to build the chapter
in one job without check-ins; the per-section stops of root rule 2, the plan
review of root rule 5 and the user picks of root rule 15 are replaced by a
plan file per section, written before the section is built and left for review
after. Each line is a setting and its value.

| Setting | Value |
|---|---|
| Chapter | 1 Essential Ideas, modules m68663 (introduction), m68664, m68667, m68670, m68674, m68690, m68683 |
| Front matter | the chapter introduction (m68663) is a page of its own in `ch01/intro/`, listed before 1.1, and the Preface (m68662) is the book's own page in `intro/`, listed before Chapter 1 (root rule 21); both are built in this pass by one agent |
| Unit of work | one section = one page; sections never folded (root rule 11), so 1.3, with two objectives and no worked example, keeps its own page |
| Order | 1.1 to 1.6 in book order, built in parallel by one agent per section |
| Loop | prep → plan file → build → validator, all six sections and the two introductions in one wave, then the chapter pass; review after |
| Prose | verbatim; the learning objectives, the section summary (to `summary_html`), the key equations and the glossary are pulled into the tables and the views (root rule 4) |
| Boxed notes | the `everyday-life` notes (Decomposition of Water / Production of Hydrogen and Chemistry of Cell Phones in 1.2, Hazard Diamond in 1.3) are kept verbatim in the text as titled asides, a `<div class="note">` with an `<h3>` of the book's own heading; their figures are treated as any figure and keep their numbers (1.15, 1.16, 1.17, 1.21). The chapter has no `sciences-interconnect` and no `chemist-portrait` note |
| Link to Learning | every one dropped and named in the section's `notes`, one plain sentence: two in 1.2 (a video on plasma; the breakdown of mercury oxide and of silver chloride) and two in 1.4 (a refresher on scientific notation; the PhET density simulation) |
| Tables | Table 1.1 (1.2), Tables 1.2, 1.3 and 1.4 (1.4), Table 1.5 (1.5) and Table 1.6 (1.6) stay in the running text as a `div.book-table` whose eyebrow is the number the publisher prints and whose caption is the book's title for it; never a `<figure>`. The unnumbered Key Equations tables of 1.4 and 1.6 are not printed: they are the equations table of `chapter.json` |
| Figure numbers | the publisher's, chapter-wide from the introduction's photograph as Figure 1.1 to Figure 1.28 in 1.6; the list is in `exploration.md` and was checked against openstax.org for 1.2, 1.3, 1.5 and 1.6 |
| Figures | the book's sketches of a quantity become interactive Figures carrying the book's number: the three beakers of 1.6 in 1.2, the nested volumes of 1.25 in 1.4, the meniscus of 1.26 and the targets of 1.27 in 1.5, the thermometers of 1.28 in 1.6. The flowcharts (1.3 and 1.4 in 1.1, 1.11 in 1.2, 1.21 in 1.3) are redrawn faithfully as still Figures with the book's numbers. A photograph is kept where the text points at it or it shows the thing the passage is about, and dropped where it is a collage opener or a stock scene; each section's plan says which and why |
| Unnumbered images | the eight inline images of 1.5 are `figure` rows with no number, whose eyebrow reads "Figure": the validator refuses a `photo` row without a number, and a `figure` row without one is the faithful copy root rule 14 asks for. They are redrawn where redrawing reads better in both themes (the significant-figure diagrams, the column sums) and copied where the reading is the point (the two graduated cylinders, the four archers) |
| Kept photographs | a `photo` row with the book's number, the bundle's image copied under its own file name into `media/ch01/`, and the book's caption kept whole including its "(credit: …)" clause. The bundle's images carry no `width`, so `widths` stays empty and no `data-width` is written |
| Sim sliders | whatever is interesting and variable in the idea: a temperature, the state of a sample, the edge of a cube and the material it is made of, the level of a meniscus, the accuracy and the precision of a group of arrows, the reading on a thermometer |
| Motion | decided per figure in its plan line with the reason (root rule 14). Nothing in this chapter has a clock in it: a beaker that answers a state slider, a cube on a balance, a meniscus, a target and a thermometer are all still pictures. A still figure registers `update: () => {}`, registers no cycle and gets no transport; a section that finds a figure with a time in it says so in its plan |
| 3D | none; every figure of the chapter is planar and drawn through `figlib` |
| Periodic table | deferred. The app has no sheet mechanism yet, so 1.3 keeps the book's own words about metals, metalloids and nonmetals, keeps Figure 1.22 as the book's image, and names the deferral in its `notes`. No page of the chapter builds a table of its own |
| Colour coding | the chapter binds `mass` and `volume` (the density figures of 1.4 and 1.5), `temperature` (1.1, 1.3 and 1.6) and `time` (1.4, where the second is named as a base unit). Everything else renders in ink: length, density, a count, a percent, a significant figure, a slope and an intercept. The element palette of the book's molecular drawings is not used in this chapter. Colour reaches the canvas only through `C()` and `PAL`, and a `\k` macro is written only for a symbol with a row in `book.json` `symbols`; `ch01/COLOR.md` holds the detail |
| Symbols | three rows added to `book.json` through `tools/mergebook.py`, the temperature-scale variants `T_C`, `T_F` and `T_K` with the macros `\kTC`, `\kTF` and `\kTK`, and one untyped row, `d` for density, with its LaTeX and no macro. No new type: mass, volume, temperature and time are already declared |
| Inline exercises | every Check Your Learning is a `check-your-learning` item placed inline right after the example it parallels, with the book's own answer from the `[answer]` note: two in 1.4, five in 1.5, five in 1.6, twelve in the chapter |
| Exercises tab | the end-of-chapter items of each module, kind `exercise`, placed at the end |
| Exercise placement | an exercise goes with the section that introduces what it tests; every item of this chapter tests what its own module teaches, so nothing is held for a later page and no row carries a `source_section` |
| Answers to book problems | the book's key only, never computed. A keyed item carries the book's answer; an unkeyed item is left out and named in the section's `exercise_notes`. "Answers will vary. Sample answer: …" (1.2's fs-idm11351120) is an open answer the reader compares with, not a graded one |
| Simulation exercises | the four PhET density items of 1.4 are kind `simulation-exercise`. The three that measure a block by displacement (fs-idm165750544, fs-idm307823136, fs-idm160286704) are carried by 1.4's own density figure once its prompts are rewritten against it, which 1.4's plan does; any the figure cannot carry, and Example 1.4.2's Check Your Learning, are left out and named in `exercise_notes` |
| Suggested approaches | generated and marked as generated, for unkeyed conceptual items only; never for a numerical item |
| Generated questions | none, of any kind; a concept node with no book exercise of its own is named in the plan and no question is written for it |
| Concept nodes | testable units only, kinds idea/result/skill, canonical book-independent kebab-case ids; 39 nodes written into `book.json` before the sections are built (3 for 1.1, 8 for 1.2, 5 for 1.3, 8 for 1.4, 7 for 1.5, 8 for 1.6) with 36 prerequisite edges, every one of them inside this chapter, since `book.json` carried no concept before this pass |
| Formulas | `ch01/chapter.json`: 6 equations (one in 1.4, five in 1.6), the book's own Key Equations important and the speed illustration of 1.6 not; 8 variable rows; no row carries an `anchor`, since an anchor into an unbuilt section fails the validator, and the chapter pass writes them from the section plans |
| Glossary | the book's own wording, 57 entries (8, 17, 6, 14, 7, 5). The two pairs the book prints twice under different heads ("homogeneous mixture" and "solution" in 1.2, "significant figures" and "significant digits" in 1.5) are kept as the book prints them, each carrying the other in its parenthetical |
| Degrees | the degree sign `°` (U+00B0), never the masculine ordinal `º` (U+00BA), in a unit string, a slider unit, an axis label or prose; inside math, `^\circ`. The book's glossary entry for `kelvin (K)` prints the ordinal ("273.15 K = 0 ºC") and is corrected to the degree sign |
| Dollar signs | `&#36;` in the prose of `text.html`, and the fullwidth `＄` inside a `prompt` or `solution` string and inside `\text{}` of a display equation; 1.6's Lamborghini example and its Check Your Learning are where this falls |
| Cross references | a reference to a section or a figure of a chapter not yet built is plain text in the book's wording. Inside the chapter, 1.5 names Table 1.4 twice and 1.6 names Table 1.2 once, each in the book's words and unlinked, since a table is not a figure row; the two references to Appendix B (1.4 and 1.5) point at the publisher's page until the app has that page |
| Voice | root rule 17 and the Voice section of the book's `RULES.md`: full sentences in a plain, measured register, on the formal side of plain, in every lead, caption, headline, readout, suggested approach, concept why and log line |
| Labels | a figure carrying the book's number is a Figure; one that replaces nothing in the book is a Sim and carries no number. The word "demo" appears nowhere |
| `ai` and `built` | `ai` names the model that did each half of the work, `{"text": …, "figures": …}`; `built` is `2026-09-12` |
| Book manifest | `ch01` first in `book.json` `chapters`, written by `tools/mergebook.py merge ch01` and never by hand |
